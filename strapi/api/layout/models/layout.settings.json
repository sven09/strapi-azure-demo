{
  "kind": "singleType",
  "collectionName": "layouts",
  "info": {
    "name": "layout",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "loginLayout": {
      "type": "enumeration",
      "enum": [
        "splitView",
        "fullWidth"
      ],
      "default": "fullWidth"
    },
    "landingLayout": {
      "type": "enumeration",
      "enum": [
        "deckView",
        "sidebarView"
      ],
      "default": "deckView"
    },
    "scheduleListViewType": {
      "type": "enumeration",
      "enum": [
        "gridView",
        "listView",
        "groupedListView",
        "horizontalView"
      ]
    },
    "scheduleListItemViewType": {
      "type": "enumeration",
      "enum": [
        "standard",
        "speaker"
      ]
    }
  }
}
